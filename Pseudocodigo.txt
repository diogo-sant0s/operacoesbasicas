Algorythm contas;

Variables A e B, sum, subtraction, division, multiplication: integer;

Beginning
        Write("Enter an integer");
    read(a);
        Write("enter another integer");
    read(b);

    sum = a + b 
    subtraction = a - b 
    division = a / b 
    multiplication = a * b 
    
        Write("Result of the sum", sum)
        Write("Result of the subtraction", subtraction)
        Write("Result of the division", division)
        Write("Result of the multiplication", multiplication)

End